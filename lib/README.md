This folder contains compatibility files.

`supabase.ts` is a temporary stub to prevent build failures if an outdated page still imports `@/lib/supabase`.
The active backend is Google Sheets via Apps Script.
